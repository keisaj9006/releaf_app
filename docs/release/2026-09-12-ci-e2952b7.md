# Completed CI verification of e2952b7

GitHub Actions [run 34710038397](https://github.com/keisaj9006/releaf_app/actions/runs/34710038397)
completed with **success** for full SHA
`e2952b7ca2f93df0bcb9c79a61465e445f47cf39`, branch `releaf-development`.
Observed through authenticated read-only `gh run view` and artifact metadata API.
The existing run was not restarted. No artifact binary or secret was downloaded.
The local `gh run watch --exit-status` process also completed with exit 0.
Documentation checkpoint checks: `git diff --check` passed; `flutter analyze`
reported no issues (exit 0, 14.4 seconds). No runtime code changed after this CI run.

## Proven scope

- Dependency resolution and lockfile check: success.
- Full bundled Sound decoding, Android source configuration and analysis: success.
- Brain, Reset model/hub/access and full test suite: success.
- All three production-manifest exports and uploads: success. Export success
  is not production narration availability or owner content approval.
- Standard and Premium Preview debug APK builds/uploads: success.
- Release AAB smoke build/upload: success, using the workflow's temporary CI
  signing identity, not the private production upload identity.
- Release 16 KB compatibility step: success. The workflow builds a release APK,
  checks ZIP alignment with `zipalign -c -P 16 -v 4`, requires native libraries,
  and checks each ELF LOAD alignment is at least 2**14. This is packaging/ELF
  evidence, not a physical 16 KB device run or Play-distributed installation.

Remote suite success is taken from the completed step result; its exact count
was not extracted from logs. Last local full suite is separately recorded as
544 passed at runtime checkpoint `fac50ec`.

## Available artifact metadata

All six artifacts reported `expired: false` when checked. Sizes/digests below
describe GitHub artifact archives, not installed app sizes or direct APK hashes.

| Artifact | ID | Archive bytes |
|---|---:|---:|
| releaf-android-debug-standard | 10303226405 | 116746050 |
| releaf-android-debug-premium-preview | 10303416103 | 116777945 |
| releaf-android-release-aab-smoke | 10303296465 | 93149600 |
| releaf-guide-production-manifest | 10303390665 | 13689 |
| releaf-reset-guide-production-manifest | 10303310816 | 25621 |
| releaf-reset-demo-production-manifest | 10302939154 | 895 |

Archive SHA-256 digests:

- Standard debug: `016f43bb2bf31737efa8625fbb2475712da35b5c6f98854143c964b329792c2b`.
- Premium Preview: `6599d9bef32e3cf9d77d81a0de7ccdcd4223faf638b40746820d388eca6afc92`.
- Release AAB smoke: `ff164f6cde2523643bf3fa4b1dfe8df1aaa340b4708a0f364c437d216faf5332`.

## Release impact and limits

The new full-decoder gate is verified on Ubuntu Actions, beyond its local
valid/corrupt fixture checks. Existing internal Android artifact checks are
also refreshed for this exact commit. No production deployment, purchase,
account deletion, Play submission or owner listening was performed.

Production signing, real Play billing/distribution, public legal resources,
store-console requirements, approved content, owner visual/audio decisions and
the production-equivalent Samsung QA matrix remain open as recorded in the
canonical release gate. Do not infer release readiness from this CI PASS.
