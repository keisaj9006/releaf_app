# Releaf 1.0 Completion Implementation Plan

**Goal:** Move the existing `releaf-development` branch from its current mature engineering state to a verified release candidate and then a public-release-ready state without uncontrolled scope expansion.

**Architecture rule:** extend existing feature modules; do not create parallel systems.

## Execution checkpoint — 11 September 2026

Continuation: audit preserved and pushed as `efbcf58`. Follow
[internal quality batches](../release/2026-09-11-internal-quality-batches.md)
for subsequent implementation and verification. External prerequisites do not
block independent product work.

See [repository audit](../release/2026-09-11-repository-audit.md) for exact evidence and file changes.

- Task 1 baseline verified: 333 tests, clean analyzer, formatter-only cleanup, web/debug Android builds, temporary-key release AAB/APK and 16 KB checks passed. No production-readiness claim.
- Task 2 live gap confirmed: deployed deletion version 1 lacks RevenueCat erasure. Repository source is hardened; real server secret, deployment and disposable-account end-to-end verification remain external.
- Tasks 3–4 existing billing/policy/deletion contracts passed; actual Play configuration, legal/controller details, public URLs and console actions remain open.
- Task 5 inventory exported: 99 of 103 meditation steps await recordings; missing exact narrator identity still blocks rendering.
- Task 6 all 16 bundled audio files decoded; Atmosphere II needs final headroom review (+0.8 dB true peak). Selection, loop listening and hardware playback QA remain open.
- Task 7 automated Brain baseline passed, including the existing portfolio and progression/lifecycle contracts. Physical-device QA remains deferred to RC.
- Tasks 8–9 are not declared complete; no research scope expansion or final-version change was made.

---

## Task 1 — Verify current baseline after latest Reset commit

**Read**
- `AGENTS.md`
- `docs/release/releaf_1_0_release_gate.md`
- `docs/CURRENT_STATE.md`

**Relevant current change**
- `lib/features/relief/presentation/breathing_widget.dart`
- `lib/theme/widgets/releaf_movement_demo_visual.dart`
- `test/reset_guidance_quality_test.dart`

**Steps**
1. Confirm branch/HEAD.
2. Run `flutter pub get`.
3. Run formatter check for changed Dart files.
4. Run focused Reset guidance/visual tests.
5. Run `flutter analyze`.
6. Run full `flutter test`.
7. Run existing non-secret release contract checks.
8. Record any drift in `docs/CURRENT_STATE.md`.

**Done when**
- automated baseline is green or every failure has a root-cause issue/plan,
- no P0 gate regressed.

---

## Task 2 — Account deletion production readiness

**Inspect**
- `lib/features/account/**`
- `supabase/functions/**`
- deletion tests
- `docs/release/releaf_1_0_release_gate.md`

**Steps**
1. Verify current client calls authenticated deletion path.
2. Verify Edge function order:
   - erase matching RevenueCat customer,
   - then delete Supabase auth user,
   - cascade dependent records as designed.
3. Verify secret is server-only.
4. Confirm repo has no secret fallback.
5. Prepare exact deployment/verification command/runbook from existing project tooling.
6. When external secret/environment is available, deploy.
7. Verify deletion end-to-end against an identified RevenueCat customer.

**Do not**
- insert `REVENUECAT_SECRET_API_KEY` into Flutter or source control,
- claim CLOSED until deployed and tested.

---

## Task 3 — RevenueCat / Play Billing finalization

**Inspect**
- `lib/core/subscription/**`
- `lib/core/paywall/**`
- relevant tests/release docs.

**Steps**
1. Reverify entitlement refresh and account-switch isolation.
2. Reverify annual/monthly package handling.
3. Reverify normalized purchase failures.
4. Reverify restore path.
5. Reverify subscription-management path.
6. Validate production key format guard.
7. Document exact required external configuration:
   - real `goog_` SDK key,
   - active Google Play products,
   - current RevenueCat Offering/packages.
8. Run purchase + restore only from a Play-distributed production-equivalent test build.

**Done when**
- code checks green,
- external config and real purchase/restore verified,
- release gate updated.

---

## Task 4 — Privacy / deletion public resources / Play policy

**Inspect**
- `lib/features/legal/**`
- `lib/core/legal/**`
- `web/delete-account.html`
- `docs/release/google_play_data_safety.md`
- `docs/release/google_play_health_declaration.md`
- `docs/release/google_play_store_listing.md`

**Steps**
1. Identify placeholder/missing controller/contact/retention details. Do not invent them.
2. Keep app copy aligned with general-wellness positioning.
3. Verify deletion page contract.
4. Prepare public HTTPS deployment.
5. Verify live URL after deployment.
6. Reconcile final Data safety mapping with actual SDK/data flows.
7. Reconcile health declaration with actual store/app claims.
8. Keep Store listing wording aligned.

---

## Task 5 — Meditation content readiness

**Inspect**
- `lib/core/audio/releaf_guide_contract.dart`
- `lib/features/meditation/**`
- `docs/meditation_audio_voice_quality_bar.md`
- `docs/releaf_guide_production*.md`
- narration assets/manifests/tests.

**Steps**
1. Inventory every meditation in current catalog.
2. Mark each as:
   - final approved narration,
   - approved script/no final recording,
   - missing/invalid audio,
   - duplicate/wrong asset.
3. Preserve 0.82× Releaf Guide direction.
4. Preserve independent narration/ambience controls.
5. Never substitute system TTS.
6. Resolve exact narrator ID if recoverable from project/provider records.
7. If not recoverable, block new production rendering and require deliberate owner audition/approval rather than guessing.
8. Run player/narration/audio lifecycle tests.

---

## Task 6 — Sleep/Sound content readiness

**Inspect**
- `lib/features/sound/**`
- `lib/features/sleep/**`
- `assets/sounds/**`
- background/interruption tests.

**Steps**
1. Inventory all 10 currently registered canonical sounds.
2. Verify each real asset exists and loads.
3. Verify loop seams/levels and metadata.
4. Verify timer.
5. Verify pause/resume/background/interruption.
6. Verify no narrator enters Sleep.
7. Finalize owner-approved content selection.
8. Keep research mixer as separate product-scope item unless 1.0 is rebaselined.

---

## Task 7 — Brain release QA, not game-count expansion

**Inspect**
- `lib/features/brain/**`
- `lib/games/**`
- `test/brain_flow_test.dart`
- Labyrinth/Memory tests.

**Steps**
1. Verify registry has one canonical entry per current game.
2. Verify every enabled game launches through canonical host.
3. Verify result screen/progress recording.
4. Verify restart/back/lifecycle.
5. Verify difficulty behavior for progressive games.
6. Verify Labyrinth 50-stage path, pause/lifecycle protections and accelerometer behavior in final device QA.
7. Audit Memory against historical progression/stat requirements.
8. Verify daily rotation/curation.
9. Do not add a new “research six” set if existing games already cover those skills.

---

## Task 8 — Research-informed UX gap audit

Do this only after baseline/release blockers are controlled.

Create a short proposal, not an immediate rewrite, for:

1. one-tap Reset / “Releaf Now” behavior,
2. intent/time-first Home,
3. consumer-facing Emergency rename with internal safety semantics preserved,
4. Leaves bonus simplification,
5. current 4-tab vs research 5-tab IA,
6. Sleep mixer scope,
7. onboarding <2 min / value-before-account,
8. privacy-minimal analytics,
9. offline audio.

For each proposal report:
- current implementation,
- research benefit,
- engineering cost/risk,
- release-gate impact,
- recommendation: pre-1.0 / 1.1 / later.

Use Superpowers brainstorming before implementation of any accepted material change.

---

## Task 9 — Release candidate

**Steps**
1. Close every remaining code-side P0.
2. Run full automated suite.
3. Set final `1.0.0+<build>` only now.
4. Build production-equivalent signed artifact using private signing configuration.
5. Run release tooling/16 KB checks.
6. Complete current Store listing assets.
7. Verify stable public privacy/deletion URLs.
8. Enter/submit Play declarations.
9. Run physical-device matrix.
10. Run Play-distributed purchase + restore.
11. Run end-to-end account deletion.
12. Complete closed-testing requirement.
13. Update release gate with evidence.

**Final condition**
Only when the canonical gate is fully closed may Codex report:

> **Releaf 1.0 is release-ready.**
